@model Normative.Models.Config.UserModel
@using Microsoft.AspNetCore.Mvc.Localization
@inject IViewLocalizer Localizer

@{
    ViewData["Title"] = "Create";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<!-- Header -->
@await Html.PartialAsync("_Header", Model.Navigation)
<!-- End Header -->
<!-- Toolbar -->
@await Html.PartialAsync("_Toolbar", Model.ToolBar)
<!-- End Toolbar -->

<h1>@Localizer["Create"]</h1>
<h4>@Localizer["User"]</h4>
<hr />

<div class="container d-flex justify-content-center mt-5">
    <div class="row w-75">

        <form asp-action="UserCreate"
              asp-controller="Settings"
              class="row"
              id="myform"
              method="post" asp-antiforgery="false">

            <div asp-validation-summary="ModelOnly" class="text-danger col-12 mb-3"></div>

            <div class="form-group col-md-6">
                <label asp-for="Request.Username" class="control-label">@Localizer["Username"]</label>
                <input asp-for="Request.Username" name="username" autocomplete="off" class="form-control" />
                <span asp-validation-for="Request.Username" class="text-danger"></span>
            </div>

            <div class="form-group col-md-6">
                <label asp-for="Request.Name" class="control-label">@Localizer["First and Last Name"]</label>
                <input asp-for="Request.Name" name="name" class="form-control" />
                <span asp-validation-for="Request.Name" class="text-danger"></span>
            </div>

            <div class="form-group col-md-6">
                <label asp-for="Request.Email" class="control-label">@Localizer["Email"]</label>
                <input asp-for="Request.Email" name="email" class="form-control" />
                <span asp-validation-for="Request.Email" class="text-danger"></span>
            </div>

            <div class="form-group col-md-6">
                <label asp-for="Request.RoleId" class="control-label">@Localizer["Role"]</label>
                <select asp-for="Request.RoleId" name="roleid"
                        asp-items="@(new SelectList(Model.Roles, "RoleId", "Name"))"
                        class="form-select">
                </select>
                <span asp-validation-for="Request.RoleId" class="text-danger"></span>
            </div>

            <div class="form-group col-md-6">
                <label asp-for="Request.Password" class="control-label">@Localizer["Password"]</label>
                <input asp-for="Request.Password" name="password" type="password" class="form-control" />
                <span asp-validation-for="Request.Password" class="text-danger"></span>
            </div>

            <div class="form-group col-md-6">
                <label asp-for="Request.ConfirmPassword" class="control-label">@Localizer["Confirm Password"]</label>
                <input asp-for="Request.ConfirmPassword" name="confirmpassword" type="password" class="form-control" />
                <span asp-validation-for="Request.ConfirmPassword" class="text-danger"></span>
            </div>

        </form>

    </div>
</div>

@section Scripts
{
    <partial name="_ValidationScriptsPartial" />

    <script>
        function Save() {
            $('#myform').submit();
        }
    </script>
}
